# 국책사업 제출본 — 제작 안내

## 산출물

| 파일 | 내용 |
| --- | --- |
| `PDF/펫나우_M1_수행결과보고서_20260821.pdf` | **제출 본편.** 표지·목차·본문·산출물 부록 29종 통합 (228쪽, 15MB) |
| `PDF/펫나우_M1_수행결과보고서_본문만_20260821.pdf` | 부록 제외 본문만 (10쪽) |
| `src/01_본책.md` | 본문 원본. 수정 후 재빌드하면 PDF에 반영됨 |
| `부록/*.md` | 부록 29종 원본. 파일명이 곧 부록 번호·제목임 |
| `부록/assets/` | 부록 F 화면 증빙 이미지 |
| `build.py` | 빌드 스크립트 |

## 제출 전 확인

- 법인 정식 명칭을 표기해야 하는 경우 `src/01_본책.md` §1의 `제출자`·`수행사` 항목 수정
- 사업명·과제번호를 표기해야 하는 경우 같은 표에 행 추가

## 재빌드

```
python3 build.py
```

- `src/01_본책.md` 또는 `부록/` 내용을 고친 뒤 실행하면 PDF 2종이 다시 생성됨
- 부록 추가·삭제는 `부록/` 폴더에 `A-9_문서명.md` 형식으로 파일을 넣거나 빼면 자동 반영됨. 단 본문 §8 산출물 총괄표는 수동으로 맞춰야 함
- 요구 환경: Python 3, `markdown` 패키지, Google Chrome

## 부록 F 화면 증빙 촬영 기준

| 항목 | 내용 |
| --- | --- |
| 대상 | 소재 A·B 랜딩페이지(모바일 화면 기준), 제품 프리토타입(데스크톱 화면 기준) |
| 기준 시점 | 광고 집행 기간 중 최종 배포 상태 (레포지터리 커밋 `a01255c`, 2026-08-18 14:53) |
| 방법 | 해당 시점 소스를 별도 경로에 복원하고 로컬에서 렌더링해 촬영 |
| 배열 | 랜딩페이지는 상단부터 하단까지 6개 구간으로 나누어 그리드로 배열 |
| 계측 처리 | 촬영 중 실 데이터 오염을 막기 위해 Mixpanel·Meta Pixel 등 계측 스크립트를 비활성화한 상태로 렌더링 |
| 광고 크리에이티브 | 집행에 사용한 원본 이미지 파일 |

- 8월 20일 이후 랜딩페이지 변경분(`Keep me posted` 선택지 숨김)은 반영되지 않은 상태이며, 이는 광고 집행 기간의 화면을 그대로 보이기 위한 의도적 선택임

## 수록 범위 판단 기준

**수록** — 고객 제출 성격의 성과물. 조사·인터뷰·제품 정의·실험 설계 및 결과·차기 계획

**미수록** — 아래는 과업 경계를 흐리거나 제출 목적과 무관해 제외함

| 제외 대상 | 사유 |
| --- | --- |
| PROJECT_STATE, 작업 로그, 리서치 리포트 구성안 | 수행사 내부 운영 문서 |
| 중간점검 자료 1·3·4, LinkedIn 후보탐색 운영키트 | 진행 과정 공유용 |
| 데모 UX 개선안 4종, 예약 퍼널 운영 문서 | 제작 과정의 내부 논의 |
| Mixpanel 사용자 삭제 기록 | 계측 도구 접속 정보 포함 |
| 리드 판정 보드 | 개별 조직 응대 이력. 결과는 부록 D-6 부록 A에 정리되어 있음 |

## 반출 정보 점검 결과 (2026-08-21)

수록 26종 전수 검사 결과 아래를 확인함.

- 접속 토큰·API 키·스프레드시트 ID 등 비밀 정보 **없음**. 부록 D-3에 등장하는 `token=…` 은 실제 값이 아닌 설명용 표기임
- 개인 이메일 주소 **없음**. 부록 문서에 나타나는 주소는 마켓핏랩 내부 주소 및 예약 도구 시스템 발신 주소임
- 유입 조직명·담당자명은 수록 상태로 유지함 (비용 집행 증빙 목적, 2026-08-21 결정)
